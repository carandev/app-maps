import { StyleSheet } from 'react-native'

const inputStyle = StyleSheet.create({
  wrapper: {
    height: 40,
    marginBottom: 50
  },
  input: {
    margin: 5,
    padding: 5,
    borderWidth: 1,
    borderColor: '#4af',
    color: '#eee'
  },
  label: {
    color: '#eee'
  }
})

export default inputStyle
